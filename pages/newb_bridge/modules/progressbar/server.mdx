# Progress bar (server)

`bridge.progressbar` has no server implementation. Accessing it on the server mounts a quiet noop table.

Use [client](/newb_bridge/modules/progressbar/client) `openprogressbar` from a client script.
