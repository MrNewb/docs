# Inputs (server)

`bridge.inputs` has no server implementation. Accessing it on the server mounts a quiet noop table.

Use [client](/newb_bridge/modules/inputs/client) `inputdialog` from a client script.
