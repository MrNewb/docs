# Text UI (server)

`bridge.textui` has no server implementation. Accessing it on the server mounts a quiet noop table.

Use [client](/newb_bridge/modules/textui/client) `showTextUI` / `hideTextUI` from a client script.
