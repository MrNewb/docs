# Target (server)

`bridge.target` has no server implementation. Accessing it on the server mounts a quiet noop table.

Use [client](/newb_bridge/modules/target/client) zone / entity / model APIs from a client script.
