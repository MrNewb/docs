# Dialogue (server)

`bridge.dialogue` is client-only. Accessing it on the server throws:

```
bridge.dialogue is only available on the client
```

Use [client](/newb_bridge/modules/dialogue/client) from a client script.
