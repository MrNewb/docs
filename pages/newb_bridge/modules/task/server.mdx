# Task HUD (server)

`bridge.task` is client-only. Accessing it on the server throws:

```
bridge.task is only available on the client
```

Use [client](/newb_bridge/modules/task/client) from a client script.
