# Menu (server)

`bridge.menu` has no server implementation. Accessing it on the server mounts a quiet noop table.

Use [client](/newb_bridge/modules/menu/client) `openMenu` / `closeMenu` from a client script.
