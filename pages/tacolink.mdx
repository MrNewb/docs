# TacoLink is now Newb_Bridge

The bridge resource was renamed from **TacoLink** to **Newb_Bridge**. Same API (`bridge.*`, `import.lua`, exports) — only the resource name changed.

Continue at [Installation](/newb_bridge/getting-started).
