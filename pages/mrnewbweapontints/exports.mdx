# Exports

These exist so ox_inventory can call `server.export` on the items. They are not a public API for other resources.

| Page | Contents |
| --- | --- |
| [Server exports](/mrnewbweapontints/exports/server-exports) | `setweapontint`, `newbserialfile` |

There are no client exports and no commands. Player use is the inventory item.
